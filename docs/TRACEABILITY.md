# Traceability

## Tickets

| Ticket | Change | Status |
|---|---|---|
| DAI-1 | Establish the dependency authority infrastructure core: seven generic modules (artifact-registry, evidence-archive, logging, network, repository-iam, recovery, workload-identity), project-level policy bindings, five parameterized trust-zone reference stacks (dep-control, dep-intake, dep-quarantine, dep-approved, dep-evidence), the OpenTofu engine convention, source-quality gates, CodeQL, dependency admission review, Dependabot, Lefthook, and importable Rulesets. | In progress |
| DAI-2 | Migrate the module path to the `t33n-software` organization namespace; add the LF line-ending contract (`.gitattributes`) and the push-protections Ruleset source `00-push-protections.json` in the verified GitHub export format. | In progress |
| DAI-3 | Align the Go 1.26.6 toolchain and source gates with the supply chain fortress contract: pinned `tools/` module with govulncheck, staticcheck, and Lefthook; fail-closed vulnerability analysis; Lefthook configuration validation and commit-msg hook; daily CI re-scan. | In progress |
| DAI-5 | Adopt the canonical repo surface with the `opentofu@1` capability pack: the byte-identical canonical callers (`ci.yml`, `codeql.yml`, `dependency-review.yml`) pinned to the repository-governance home at the provision-seam stand; the thin `canonical-conformance.yml` lane running the home verifier; the canonical file family and the materialized `.github/CODEOWNERS`; the `repo-bindings.json` tenant manifest; the schema-v4 quality configuration declaring `extends: ["opentofu@1"]`; the tooling-module pins for `quality-gate` and `check-coverage` (the provision-capable go-quality-authority stand), `verify-canonical` (the extends-capable repository-governance stand), and the shared-kernel module requirement for the pack registry resolution; the conventional `--version` surface of the development tools; the OpenTofu gates moved into the pack with the repository-local setup steps and the duplicated engine convention document removed; and the packaging contract bound to the manifest. | In progress |
| DAI-6 | Reference the canonical gate chain through the tooling module pin: the repo-local chain copies `cmd/build` and `cmd/check-coverage` are removed (the `cmd/` tree carries no repository tooling anymore), the quality configuration invokes the go-quality-authority orchestrator via `go tool -modfile tools/go.mod quality-gate` with the `opentofu@1` pack declaration unchanged, the restated `defaults` block is dropped for the schema-owned `includeFamilies` default (SCG-9), and the contract guards prove the canonical invocation, the absent `defaults` block, and the absence of both copies fail-closed. | In progress |
| DAI-7 | Onboard the license-hub render-and-verify lane in the strengthened unit form: the tenant values `license.values.json` and the digest-pinned lock `license.lock.json` (template `license-hub/templates/custom/norepublish/NoRepublish-1.0.0.hbs`, version 1.0.0), the rendered `LICENSE` and `LICENSES/LicenseRef-dependency-authority-infrastructure-NoRepublish-1.0.txt` instance proven byte-identical against the canonical render, the binding manifest flip `licenseHub: true`, the coupled tooling-module pins for the catalog-admitted license CLI, the license-content-proof verifier, and the go-quality-authority catalog stand, the three callers converged byte-identical to the reissued canonical pin, and the conformance lane re-bound to it — activating the merge-blocking license content proof inside the canonical conformance check. | In progress |
| DAI-9 | Extend the artifact-registry module with the DOCKER workload image class: the format enum carries DOCKER, DOCKER always binds STANDARD_REPOSITORY mode and never carries a remote upstream; the dep-control reference stack declares both control-zone workload image registries (`staging-controller-images` as the only governed producer delivery target and `release-controller-images` as the only workload consumption source, filled exclusively through promotion) with boundary and zone labels, the instance-supplied location input and the class-keyed repository outputs; the packaging contract guards bind the class rules and the stack declarations fail-closed. | In progress |
| DAI-10 | Declare the canonical workload job topology of the in-perimeter execution substrate: the new generic `cloud-run-job` module (one Cloud Run job per lane operation, executed as the existing zone workload identity, consuming only a full immutable `@sha256:` digest from the release-class workload image registry, with the fail-closed digest and release-class validations); the atomic seven-job declaration across the three zone reference stacks (`dep-intake-fetch` in dep-intake; `dep-admission`, `dep-promotion`, `dep-revalidation`, `dep-revocation` in dep-control; `dep-evidence-write`, `dep-evidence-audit` in dep-evidence) with the image digests as instance-supplied bindings without stack defaults (planned until the promotion read-back proof flips them to bound); the class-keyed job outputs; and the packaging contract guards that bind the module form, the complete zone-pure topology and the absent stack defaults fail-closed. | In progress |
| DAI-11 | Declare the canonical IAM target matrix of the dependency authority: the repository-scoped data-plane roles of the eight zone workload identities bound per the identity model — the intake stack gains the instance-wired additional reader members (canonically the admission and promotion controllers of the control zone), the evidence stack gains the additional writer members (the admission, revalidation and revocation controllers) with the approved promoter bound as an additional auditor reader, the approved stack is realigned to the member-input form (the zone-local promoter identity is removed, the zone pool is created without identities by default, and the promoter and revocation members write while the revalidation member reads), and the control stack binds the release-class workload image registry read for all seven lane identities (the four control-plane lanes plus the cross-zone member input) with no writer on either class and no binding on the staging class; the architecture decision record carries the canonical matrix including the quarantine, break-glass and scanner exclusions; and the packaging contract guards bind every row and every forbidden surface fail-closed. | In progress |
| DAI-12 | Declare the invoke-only trigger rights of the external trigger seam: the workload-identity module creates a dedicated invoke-only trigger service account per lane identity (`dep-<operation>-trigger`), the principal-set binding federates the trigger identity and never the execution identity, and the trigger identity never carries a role; the cloud-run-job module binds the lane trigger identity through the new `invoker_member` input with `roles/run.invoker` (the invoke permission `run.jobs.run`) and `roles/run.viewer` (the execution status read-back) resource-scoped to exactly its own job, with the role contents proven against the provider; the three zone reference stacks declare the seven canonical trigger identities and wire them to their jobs; the stack outputs export the trigger identity emails; the architecture decision record carries the trigger identity form including the no-data-plane, no-project-level-invoke and no-shared-trigger exclusions; and the packaging contract guards bind every declaration and every forbidden surface fail-closed. | In progress |
| DAI-13 | Declare the workload network origin form and correct the trigger role form: the cloud-run-job module binds the lane trigger identity with `roles/run.jobsExecutorWithOverrides` (the lane passes the operation inputs as execution-parameter overrides, so the invoke call is an override execution requiring `run.jobs.runWithOverrides`) plus `roles/run.viewer`, and carries the mandatory zone network attachment (`vpc_access` with the hardcoded `ALL_TRAFFIC` egress and the network and subnetwork as mandatory, fail-closed validated instance inputs); the network module gains the zone workload network origin surface (exactly one VPC with one Private Google Access subnetwork in the job region, the restricted-range DNS response policy resolving `*.googleapis.com` to `restricted.googleapis.com` on `199.36.153.4/30`, and the egress firewall pair ordered around priority 1000); the policy bindings gain the opt-in Cloud Run organization policies (`run.allowedVPCEgress` allows only `all-traffic`, `run.allowedIngress` allows only `internal`), enabled by default in the three job-zone reference stacks; the three job-zone stacks declare their zone network through the network module and wire it into their workload jobs, while the job-free zones stay network-free (zone purity); the canonical IAM target matrix binds the intake fetcher's writer grant on the evidence repositories (the intake use case writes its candidate records there); the architecture decision record carries the corrected role form, the completed matrix and the network origin decision; and the packaging contract guards bind every declaration and every forbidden surface fail-closed. | In progress |

| DAI-14 | Declare the forensics reader access class of the operator access classes: the new generic `forensics-readers` module binds the organization-owned forensics group (the canonical identity class `dep-forensics-readers`, supplied by the organization instance, never a core literal) with exactly `roles/logging.viewer` and `roles/run.viewer` project-scoped on the zone project and no other grant on any plane, and declares the second, separate perimeter ingress rule — the forensics group as the only identity, scoped to the read-only logging method `logging.logEntries.list` with the zone projects as resources, through the same identity-bound channel as the administration rule — as a null-gated surface validated fail-closed; all five zone reference stacks wire the instance-supplied group, the control-zone stack additionally binds the perimeter rule as a required instance input (declared exactly once) while the other stacks never carry the rule surface; the architecture decision record carries the access-class decision including its exclusions; the packaging contract guards bind every declaration and every forbidden surface fail-closed; and the invoke-only trigger rights sweep carries the single documented exception of the forensics reader access class, whose exact surface the new guard pins fail-closed. | In progress |

| DAI-15 | Correct the forensics perimeter ingress rule to the platform-supported method-selector form: the live perimeter update proved that Cloud Logging supports only the method form (the platform rejects the permission-selector form fail-closed), so the `forensics-readers` module now declares the read-only logging scope as `method = "LoggingServiceV2.ListLogEntries"`; the module comment, the module and dep-control stack READMEs and the architecture decision record carry the method form; and the packaging contract guard binds the method form fail-closed and gains a regression guard that forbids the permission selector. | In progress |

| DAI-16 | Correct the workload network origin to cover the Artifact Registry data plane: the network module's restricted-range DNS response policy gains the second rule `restricted-pkg-dev` resolving `*.pkg.dev` to the restricted VIP record set (`199.36.153.4`, `199.36.153.5`, `199.36.153.6`, `199.36.153.7`; TTL 300) — the registry domains are served by the restricted range, and without the mapping the workload's registry calls (the intake fetch from `<region>-go.pkg.dev`) resolved to public addresses and failed against the deny-all egress rule, as proven by the diagnosed timeout of the live intake execution; the egress firewall pair is unchanged because its allow rule targets the restricted range, not a domain; the module contract surface, the module README and the three job-zone stack READMEs carry the data-plane coverage; the architecture decision record carries the corrected network origin form; and the packaging contract guard binds the second rule, its DNS name, the shared record set and the exact two-rule count fail-closed. | In progress |

| DAI-17 | Declare the remote upstream allowance of the intake zone and the governed dual-provider form of the declaration plane: the provider supply chain carries the exactly pinned `hashicorp/google-beta` beside the GA pin (the same publisher and signing trust anchor) for exactly one proven case — the Artifact Registry VPC Service Controls configuration exists only in the beta provider while its API is generally available, and the pinned GA provider schema provably lacks the resource; the artifact-registry module declares the allowance as a null-gated opt-in surface (`vpcsc_upstream_allowance`, default fail-closed denied, remote mode required) binding `google_artifact_registry_vpcsc_config` with `vpcsc_policy = "ALLOW"` scoped to the project and location; the intake reference stack opts in while the four other stacks never wire it (zone purity); every stack pins and configures the beta provider uniformly; the architecture decision record carries the dual-provider decision including the GA-promotion flip path and the corrected engine-rationale reference; the standing rule lives in the new provider-binding convention document; and the packaging contract guards bind the surface, the pin scope, the beta scope and the zone purity fail-closed. | In progress |

| DAI-18 | Declare the revalidation controller's intake read in the canonical IAM target matrix: the revalidation lane materializes the candidate content from the controlled intake boundary (the module archive fetch proven against the recorded candidate digest), so the matrix binds the revalidation controller as a canonical reader of the intake repositories beside the admission controller and the approved promoter; the dep-intake reference stack documents the extended canonical reader set in the member input and its README; the architecture decision record carries the extended matrix row with its reason; and the packaging contract guard binds the new matrix row fail-closed. | In progress |

| DAI-19 | Bind the approved promoter's evidence write in the canonical IAM target matrix: the promotion lane writes its approved record into the evidence repository itself (the evidence-writer boundary — a lane never externalizes its domain evidence), so the matrix binds the approved promoter as a canonical writer of the evidence repositories beside the admission, revalidation and revocation controllers and the intake fetcher, replacing the retired canonical reader form (the writer role already carries the read capability); the dep-evidence reference stack documents the extended canonical writer set in the member input and its README; the architecture decision record carries the corrected matrix row with its reason; and the packaging contract guard binds the new writer row fail-closed and forbids the retired reader form. | In progress |

| DAI-20 | Declare the consumer verification lane in the infrastructure core: the control reference stack gains the fifth canonical workload job `dep-consumer-verification`, bound to the new `consumer-verification` lane identity and invoked only through its dedicated invoke-only trigger identity `dep-consumer-verification-trigger`; the canonical IAM target matrix gains the consumer verifier row — `dep-consumer-verifier` reads the approved repositories as the consumer read path of the consumer verification lane and writes its own lane evidence into the evidence repositories (both standing and repository-scoped, never a mutation-window grant) and reads the release-class workload image registry like every lane identity; the evidence and approved reference stacks document the extended canonical writer and consumer reader sets in their member inputs and READMEs; the architecture decision record carries the extended matrix; and the packaging contract guards bind the complete eight-job topology, the eight invoke-only trigger identities and the new matrix row fail-closed. | In progress |
| DAI-21 | Declare the control-zone state home in the infrastructure core: the new generic `state-home` module (the dedicated zone state bucket with object versioning, uniform bucket-level access, enforced public access prevention and the mandatory bucket CMEK of the dual fortress provider layer — never a retention policy, because the state layer is the recovery root, not an archive — plus exactly the `roles/storage.objectAdmin` operator data plane, resource-sharp on the bucket, with fully formed instance-supplied members); the new minimal `dep-control-state` state-home root (the engine layer of the dual fortress form — the `gcp_kms` key provider with `key_length = 32`, the `aes_gcm` method, the fail-closed `enforced` state and plan blocks, the `remote_state_data_sources` default read edge and the stable `encrypted_metadata_alias` — and the `gcs` backend binding with the state-key grammar prefix `dep-control-state`, with the bucket name, the two cryptographically disjoint key references and the operator members as mandatory instance bindings, the disjointness and the location coupling proven fail-closed); the committed stack lockfile; the architecture decision record carrying the state-home decision; and the packaging contract guards binding the module form, the root form, the minimal-root purity and the absent defaults fail-closed. | In progress |
| DAI-22 | Rebuild the zone state homes as foundation-provisioned consumption: the zone self-provisioning form is retired (the `dep-control-state` state-home root and the `state-home` module are removed — the foundation layer of the developer platform infrastructure core declares every zone state bucket in the dual fortress form), and every zone reference stack (`dep-control`, `dep-intake`, `dep-quarantine`, `dep-approved`, `dep-evidence`) carries the final `gcs` backend into the instance-bound zone state bucket (the state-key grammar prefix identifying exactly the root) plus the engine layer of the dual fortress state-encryption standard (the `gcp_kms` key provider with `key_length = 32`, the `aes_gcm` method, the fail-closed `enforced` state and plan blocks, the `remote_state_data_sources` default read edge and the stable `encrypted_metadata_alias`), with the bucket name and the engine key reference as mandatory instance bindings without defaults; the architecture decision record carries the foundation-provisioned state architecture; the stack READMEs carry the consumption boundary and the new inputs; and the packaging contract guard binds the final backend and encryption form of every zone stack, the absent defaults and the retired self-provisioning surfaces fail-closed. | In progress |

| DAI-23 | Correct the state_bucket_name validation of the five zone reference stacks to the documented string-matching form: the google-spelling clause bound the membership test `!contains(var.state_bucket_name, "google")`, whose first argument is a string and which therefore errors only when a value is evaluated — passing format, initialization and validation silently (the proven defect class of the OpenTofu quality-gates reference) — and now binds `!can(regex("google", var.state_bucket_name))`; every zone stack carries its behavioral proof `variables.tofutest.hcl` (one acceptance run and the four rejection runs of the naming rules through expect_failures, executed in the governed execution window because the encryption-carrying roots never initialize offline); and the packaging contract guard binds the corrected form, the retired defect form and the per-root proof files fail-closed. | In progress |
| DAI-24 | Complete the control-stack declaration against the proven reality inventory: every job-owning stack (dep-intake, dep-control, dep-evidence) binds the workload job activation gate of the operating model's engine-active surface composition — the required instance-bound `enabled_workload_jobs` input (never a stack default) with the fail-closed consistency validation against the declared zone topology (the unidirectional form proven against the pinned engine: a bidirectional cross-variable validation pair cycles the evaluation graph), the filtered job module `for_each`, and the per-root behavioral proofs (the acceptance run and the rejection run of an unknown enabled job, executed in the governed execution window) — and the control stack declares the recovery identity of the canonical IAM target matrix through the recovery module (the dedicated identity whose elevated project role exists only under the mandatory time-bound IAM condition, never federated from CI and never carrying a data-plane grant), with the role and the end time as approved instance decisions through the new `break_glass_recovery` input, the identity email output, and the rejection run of an invalid end time; the architecture decision record carries the activation gate and the recovery declaration; and the packaging contract guards bind the gate, the zone purity and the recovery declaration fail-closed. | In progress |
| DAI-25 | Declare the workload image cleanup lifecycle surface: the artifact-registry module's cleanup policy surface is proven against the pinned provider schema (google 7.44.0, `tofu providers schema -json`) and binds the class rule fail-closed (only DOCKER workload image repositories carry cleanup policies; the dependency repositories and the evidence plane are append-only supply-chain records), with the corrected convention-form description and the module-level behavioral proofs executed offline; the control reference stack binds the two workload image registries through the required instance-bound `workload_image_cleanup` input (the per-class policy content and the dry-run activation state, never a stack default) with the convention's structural rules proven fail-closed (the release class carries keep policies only; the staging class carries the time-based delete plus the keep floor) and the stack-level behavioral proofs; the architecture decision record carries the lifecycle declaration; and the packaging contract guards bind the surface, the class rule, the stack wiring, the zone purity and the documentation fail-closed. | In progress |
| DAI-26 | Declare the break-glass recovery identity of every trust zone: the four stacks dep-intake, dep-quarantine, dep-approved and dep-evidence gain the same recovery surface as the control stack — the required instance-bound `break_glass_recovery` input (the project-level role under the mandatory time-bound IAM condition and the RFC 3339 end time, both validated fail-closed, never a stack default), the recovery module instantiation bound to the zone's own project, the identity email output, the README boundary and the behavioral rejection proof beside every stack — so every zone carries its dedicated, dormant, time-bounded recovery identity exactly once; the architecture decision record carries the per-zone decision (the matrix row and the activation-gate item evolve to the every-zone form, and the new decision 15 binds the per-zone convention); and the packaging contract guard binds the every-stack declaration, the own-project binding, the outputs, the proofs and the documentation fail-closed. | In progress |
| DAI-27 | Declare the mandatory description surfaces of the declaration plane: the network module exposes the five description surfaces of the workload network origin (the VPC and the subnetwork as create-only surfaces, the egress firewall pair and the restricted-range DNS response policy as in-place surfaces — the DNS response policy rules carry no description surface in the pinned provider schema, proven through `tofu providers schema -json`) as optional inputs with the null-or-non-empty validation and the offline behavioral proofs; the control stack binds them as required instance-supplied values (byte-exact to the live values on the create-only surfaces) with the fail-closed validation and the stack-level behavioral proofs; the workload-identity module binds the trigger identity display name to its identity class name; the control stack binds the pool display and description surfaces, the audit sink description and the per-controller display name and description as required, validated values; the architecture decision record carries the mandatory description duty decision; and the packaging contract guard binds the module surface, the stack bindings, the identity surfaces, the proofs and the documentation fail-closed. | In progress |
| DAI-28 | Declare the workload job environment ownership surface: the control reference stack binds the static, non-credential configuration of the zone's workload jobs through the required instance-bound `workload_job_env` input (the static environment bindings keyed by the canonical job name, never a stack default) with the fail-closed validations (the input references only declared jobs of the zone topology, every key is a well-formed environment name, and no key or value ever carries a credential marker), wires it into the job module (`env = lookup(var.workload_job_env, each.key, {})`), and carries the behavioral proofs (the acceptance run and the rejection runs of an unknown job and a credential-carrying binding); the architecture decision record carries the workload configuration ownership decision; and the packaging contract guard binds the input, the wiring, the zone purity, the proofs and the documentation fail-closed. | In progress |
| DAI-29 | Carry the convergence window import carrier as an ignored working file: the `.gitignore` project block ignores `window.imports.tf` below the project-additions mark, so the plan-driven declarative import blocks of a convergence window live in a normal git-ignored file that is removed after the zero-drift proof (the imperative import stays forbidden by the operating model, and override files provably reject import blocks), and the packaging contract guard pins the pattern in the preserved-patterns list fail-closed. | In progress |
| DAI-31 | Flip the quality-engine pin of the tools module to the merged engine-machinery line: the tools module now requires go-quality-authority v0.0.0-20260923180700-e24f6bb77ccd (the merged develop tip carrying the controlled pack-gate environment, the bounded failure-output surfacing, and the engine-machinery currency enforcement), so the repository quality gate runs the pack gates under the governed environment instead of the uncontrolled operator-process inheritance that failed deterministically at the dep-control root initialization; the transitive tool-channel requirements settle to the engine manifest (x/mod v0.41.0, x/sync v0.23.0, x/text v0.42.0). | In progress |

## Scope boundaries

- DAI-1 delivers the organization-agnostic source core only. It does not
  create Google Cloud projects, enable APIs, or provision live trust-zone
  resources; the organization instance provisions those through the governed
  infrastructure change.
- Revocation download rules are runtime operations of the revocation
  controller and are intentionally not part of the static stacks.
- The core contains no concrete organization or tenant bindings, no live
  state or plans, and no variable binding files.
- The `release/*` and `support/*` branch families and their Rulesets are
  activated only with a complete governed release lifecycle.
